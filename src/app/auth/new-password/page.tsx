import NewPasswordView from "@/sections/auth/NewPasswordView";

export const metadata = {
  title: 'New Password',
};

const NewPasswordPage = () => {
  return (
    <NewPasswordView />
  )
}

export default NewPasswordPage;